<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SteveCadwallader.CodeMaid.UI.Dialogs.Options.Reorganizing"
        xmlns:p="clr-namespace:SteveCadwallader.CodeMaid.Properties"
        xmlns:cnv="clr-namespace:SteveCadwallader.CodeMaid.UI.Converters">
    <DataTemplate DataType="{x:Type local:ReorganizingByAttributesViewModel}">
        <StackPanel>
            <TextBlock Text="{x:Static p:Resources.ReorganizingByAttributes_Text}" Margin="5,5,5,0" />
            <TextBox Style="{DynamicResource TextBoxMultiLine}"
                     Text="{Binding SortByAttributesExpression, Converter={x:Static cnv:StringReplaceConverter.DoublePipeToNewLine}, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>